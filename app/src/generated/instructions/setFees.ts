/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU16Decoder,
  getU16Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from '@solana/kit';
import { C_SPL_WRAPPER_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const SET_FEES_DISCRIMINATOR = new Uint8Array([
  137, 178, 49, 58, 0, 245, 242, 190,
]);

export function getSetFeesDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(SET_FEES_DISCRIMINATOR);
}

export type SetFeesInstruction<
  TProgram extends string = typeof C_SPL_WRAPPER_PROGRAM_ADDRESS,
  TAccountWrapperConfig extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountWrapperConfig extends string
        ? WritableAccount<TAccountWrapperConfig>
        : TAccountWrapperConfig,
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      ...TRemainingAccounts,
    ]
  >;

export type SetFeesInstructionData = {
  discriminator: ReadonlyUint8Array;
  wrapFeeBps: number;
  unwrapFeeBps: number;
};

export type SetFeesInstructionDataArgs = {
  wrapFeeBps: number;
  unwrapFeeBps: number;
};

export function getSetFeesInstructionDataEncoder(): FixedSizeEncoder<SetFeesInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['wrapFeeBps', getU16Encoder()],
      ['unwrapFeeBps', getU16Encoder()],
    ]),
    (value) => ({ ...value, discriminator: SET_FEES_DISCRIMINATOR })
  );
}

export function getSetFeesInstructionDataDecoder(): FixedSizeDecoder<SetFeesInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['wrapFeeBps', getU16Decoder()],
    ['unwrapFeeBps', getU16Decoder()],
  ]);
}

export function getSetFeesInstructionDataCodec(): FixedSizeCodec<
  SetFeesInstructionDataArgs,
  SetFeesInstructionData
> {
  return combineCodec(
    getSetFeesInstructionDataEncoder(),
    getSetFeesInstructionDataDecoder()
  );
}

export type SetFeesInput<
  TAccountWrapperConfig extends string = string,
  TAccountAuthority extends string = string,
> = {
  wrapperConfig: Address<TAccountWrapperConfig>;
  authority: TransactionSigner<TAccountAuthority>;
  wrapFeeBps: SetFeesInstructionDataArgs['wrapFeeBps'];
  unwrapFeeBps: SetFeesInstructionDataArgs['unwrapFeeBps'];
};

export function getSetFeesInstruction<
  TAccountWrapperConfig extends string,
  TAccountAuthority extends string,
  TProgramAddress extends Address = typeof C_SPL_WRAPPER_PROGRAM_ADDRESS,
>(
  input: SetFeesInput<TAccountWrapperConfig, TAccountAuthority>,
  config?: { programAddress?: TProgramAddress }
): SetFeesInstruction<
  TProgramAddress,
  TAccountWrapperConfig,
  TAccountAuthority
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? C_SPL_WRAPPER_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    wrapperConfig: { value: input.wrapperConfig ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.wrapperConfig),
      getAccountMeta(accounts.authority),
    ],
    data: getSetFeesInstructionDataEncoder().encode(
      args as SetFeesInstructionDataArgs
    ),
    programAddress,
  } as SetFeesInstruction<
    TProgramAddress,
    TAccountWrapperConfig,
    TAccountAuthority
  >);
}

export type ParsedSetFeesInstruction<
  TProgram extends string = typeof C_SPL_WRAPPER_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    wrapperConfig: TAccountMetas[0];
    authority: TAccountMetas[1];
  };
  data: SetFeesInstructionData;
};

export function parseSetFeesInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedSetFeesInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { wrapperConfig: getNextAccount(), authority: getNextAccount() },
    data: getSetFeesInstructionDataDecoder().decode(instruction.data),
  };
}
